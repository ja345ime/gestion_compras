Agrega un botón en la vista de requisiciones que diga "Imprimir PDF" y que use una función ya existente llamada generar_pdf_requisicion().
Quiero que apliques tú mismo los cambios en el sistema, no solo me digas qué hacer.

Agrega un botón "Imprimir PDF" en la vista de requisiciones y haz que al hacer clic se llame la función `generar_pdf_requisicion(requisicion_id)`. Usa la lógica real de Flask como la que ya está implementada en el sistema.

Usa este formato:

```json
{
  "app/routes/requisiciones.py": "contenido completo nuevo de este archivo",
  "app/utils/auditoria.py": "contenido completo nuevo de este archivo si lo modificas",
  "app/templates/requisiciones.html": "nuevo contenido del HTML si se modifica"
}
No me des explicaciones, solo el JSON válido con los archivos a sobrescribir. Si no necesitas modificar un archivo, no lo pongas en el JSON.

yaml
Copiar
Editar

---

### Luego:

1. Guarda ese texto en `prompt_actual.txt`
2. Ejecuta de nuevo:

```bash
python3 automatizador_codex.py
